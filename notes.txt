NVM: Node Version Manager
LTS: Long Term Support, (suporte mais extendido para essa versão)
rc: run commands (convensão de scripts de inicialização)
npm: gerenciador de pacote
next: gerenciar o back-end e front-end
react: irá construir tudo

nvm ls
nvm install lts/hydrogen
nvm install (o arquivo .nvmrc irá completar)
node -v
nvm --help
nvm alias default lts/hydrogen // garante que a versão não mude

npm init
npm install next@13.1.6
npm install react@18.2.0
npm install react-dom@18.2.0

1. Ao informar este comando, o sistema irá procurar no arquivo package.json a chave scripts e o valor dev, e assim executar o comando
npm run dev

2. limpa o terminal
ctrl + l
npm install prettier -D // padronizar a estilização do código + devDependencies (-D) no JSON

npm install --save-dev jest@29.6.2
npm run + scripts do json

usar no navegador: JSON Viewer Pro

1. Ao ligar o servidor: npm run dev, abrir nova janela do bash e digitar: curl http://localhost:3000/api/status -v, sera apresentado o protocolo http

nslookup web-course-iota.vercel.app
curl https://64.29.17.193 --insecure -verbose

1. Um servidor que hospeda diversos sites irá retornar o site que esta no Header Host, que por padrão é o endereço IP, deverá ser alterado pelo dominio
curl https://64.29.17.193 --insecure -verbose --header 'Host: https://web-course-iota.vercel.app/'

docker-compose -v
docker compose up
docker ps
docker ps -a
docker compose up -d ou --detach (rodar em background e usar o terminal)
sudo apt update (atualizar a lista dos gerenciadores de pacotes)
sudo apt install postgresql-client (instalar)
docker compose up -d --force-recreate (receriar docker para realizar a conexao)
psql --host=localhost --username=postgresql --port=5432 (realiza a conexao)
\q
docker compose down

docker compose -f infra/compose.yaml up